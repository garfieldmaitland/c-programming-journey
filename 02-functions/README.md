# String Length Counter

My string length counter function

## What I learned
- I already knew that there are different ways to get similar functional output, for different program implementations
- I was able to see it with a very simple function strlen
- I am 80% confident this program exists as a function/method in python, but I wanted to implement it from scratch
- In doing so I learned that the implementation can be done at the array level or the pointer level
- The pointer level is reminicent of leetcode
- I want to continue to decompose the pointer implementation
- I want to calculate the time complexity of the pointer implementation, my intuition is that it is O(n) given that there a loop that advances the pointer incrementally until the termination value, and then a difference operation is done to calculate the final value